<div class="comment">
  	<div class="comment-lead">
      On <%= strip_zeros_from_date(comment.created_at.strftime("%B *%d, %Y")) %>,
         <b><%= h(comment.author) %></b> wrote
    </div>
        <div class="comment_text"><%= prepare_post_body(comment.content) %></div>
</div>
