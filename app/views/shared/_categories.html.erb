<div id="categories">
	<h4>Categories</h4>
	<ul>
	<% @all_categories.each do |cat| -%>
		<li><%= link_to(cat.name, category_path(cat)) %></li>
<%#:action => 'category', :id => cat.id%>
	<% end -%>
	</ul>
</div>
